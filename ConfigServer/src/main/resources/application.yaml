server:
  port:
spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri:
          clone-on-start: true

eureka:
  instance:
    preferIpAddress: true
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone:
